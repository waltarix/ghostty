name: Release

on:
  push:
    tags:
      - v[0-9]+.[0-9]+.[0-9]+-custom
      - v[0-9]+.[0-9]+.[0-9]+-custom-r[0-9]+

jobs:
  build:
    runs-on: macos-26
    env:
      xcode_version: "26.0"
      zig_version: 0.14.1

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install zig
        run: |
          mkdir -p opt/zig
          cd opt
          curl -L 'https://ziglang.org/download/${{ env.zig_version }}/zig-aarch64-macos-${{ env.zig_version }}.tar.xz' \
            | tar -Jxf - --strip-components=1 -C zig
          echo "$(pwd)/zig" >> $GITHUB_PATH

      - name: Setup Xcode
        run: sudo xcode-select -s '/Applications/Xcode_${{ env.xcode_version }}.app/Contents/Developer'

      - name: Install MetalToolchain
        run: xcodebuild -downloadComponent MetalToolchain

      - name: Unarchive ziglyph-custom
        run: tar -Jxf deps/ziglyph-custom.tar.xz -C deps/

      - name: Build
        run: zig build -Doptimize=ReleaseFast

      - name: Archive
        run: tar -C zig-out -Jcvf Ghostty.tar.xz Ghostty.app

      - name: Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "*.tar.xz"
          allowUpdates: true
